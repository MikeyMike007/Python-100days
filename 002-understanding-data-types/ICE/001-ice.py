# following code converts a string number into an int number and then adds them
# together and prints the result to stdout
two_digit_number = input("Type a two digit number: ")
print(int(two_digit_number[0])+int(two_digit_number[1]))
